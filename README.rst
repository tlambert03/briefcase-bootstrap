napari bundle bootstrap
=======================

a basic example of an app bundle that bootstraps napari.  The "app" here is 
just a shell around a pip installer.  This allows updating (or downgrading!)
napari itself.
